import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  iconContainer: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
