import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    width: '100%',
    height: '100%',
  },
  map: {
    position: 'relative',
    top: 0,
    right: 0,
    bottom: 0,
    left: 0,
    height: '100%',
    width: '100%',
  },
  refreshButton: {
    justifyContent: 'center',
    marginTop: '-1%',
  },
});
